#!/bin/sh
# Startup iptables rules — runs on every boot
# Wait for network interfaces to be ready
sleep 10

# Redirect port 80 to captive portal Flask app
iptables -t nat -A PREROUTING -i br-lan -p tcp --dport 80 -j REDIRECT --to-port 80

exit 0
